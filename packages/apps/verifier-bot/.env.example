# Verifier Bot Environment Variables

# REQUIRED: Your verifier's API key from the Audit Hub
# Generate this from the verifier dashboard UI after depositing stake
# Format: vr_<32-character-hex-string>
VERIFIER_API_KEY=vr_0123456789abcdef0123456789abcdef

# Network to connect to (ic for mainnet, local for testing)
IC_NETWORK=ic

# How often to poll for pending verifications (milliseconds)
POLL_INTERVAL_MS=60000

# Maximum time for a single build (milliseconds)
BUILD_TIMEOUT_MS=600000
